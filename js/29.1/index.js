/**
 * 1. Require module math
 * 2. S? d?ng method `add` d? tính t?ng 2 s? b?t kì
 * 3. Log ra k?t qu? phép tính t?ng ? trên
 */

var Math = require('./math');

Math.add(1,2);

console.log(Math.add(1,2))

