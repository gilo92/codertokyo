/**
 * 1. Thêm method multiply vào module `math` nh?n vào 2 s? b?t kì, tr? v? tích 2 s?
 * 2. Require module math và tính tích 2 s? b?t kì dùng method multiply dã t?o ? trên
 * 3. Log ra k?t qu?
 */

var Math1 = require(`./math`);

console.log(Math1.multiply(2,3));
